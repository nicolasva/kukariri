%h1
  = "Suggestions d'amis"
%table.table
  %tr
    %th
      = "E-mail de l'ami suggèrer"
    %th
      = "Annuler"
    %th
      = "Ajouter en tant qu'ami"
    %th 
      = "Le suivre"
  :each contact in contacts
    %tbody
      %tr
        %td = contact.user.email
        %td{class: "cancel_friend", id: "cancel_friend_"+contact.user.id} 
          %span.btn.btn-danger
            = "Annuler"
        %td{class: "add_friend", id: "add_friend_"+contact.user.id} 
          %span.btn.btn-primary
            = "Ajouter en tant qu'ami"
        %td{class: "follow_person", id: "follow_person_"+contact.user.id}
          %span.btn.btn-success
            = "Suivre uniquement cette personne"
